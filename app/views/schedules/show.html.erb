<div class="container-fluid">
  <h1 class="ls-title-intro ls-ico-calendar">Atividade</h1>
  <div class="ls-list">
    <header class="ls-list-header">
      <div class="col-md-9">
        <div class="ls-list-title ">
          <a href="#">Serviços atribuidos</a>
        </div>
        <div class="ls-list-description">
          <br>
          <ul>
            <% @schedule.services.each do |service| %>
              <li><%= service.title %></li>
            <% end %>
          </ul>
        </div>
      </div>
      <div class="col-md-3 ls-txt-right">
        <!-- <a href="&#38;lt;#" class="ls&#45;btn&#45;primary">Editar</a> -->
      </div>
    </header>
    <div class="ls-list-content ">
      <div class="col-xs-12 col-md-6">
        <span class="ls-list-label">Data inicio</span>
        <strong><%= @schedule.date_start %></strong>
      </div>
      <div class="col-xs-12 col-md-6">
        <span class="ls-list-label">Data termino</span>
        <strong><%= @schedule.date_end %></strong>
      </div>
    </div>
  </div>
</div>
