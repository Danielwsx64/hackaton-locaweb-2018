<div class="container-fluid">
  <h1 class="ls-title-intro ls-ico-cog">Configurações</h1>
  <% @settings.each do |settings| %>
    <b>Horas disponiveis no dia:</b> <%= settings.work_hours %>
  <% end %>
</div>
<p id="notice"><%= notice %></p>

<%= link_to 'New Setting', new_setting_path %>
